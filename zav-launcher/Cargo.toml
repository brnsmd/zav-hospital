[package]
name = "zav-launcher"
version = "0.1.0"
edition = "2021"
description = "ZAV Hospital System Launcher - starts all services and Boss TUI"
authors = ["Zav Team"]

[dependencies]

[build-dependencies]
winres = "0.1"

[profile.release]
opt-level = "s"
lto = true
strip = true
